<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- Parent multi-module project -->
    <parent>
        <groupId>com.ipm</groupId>
        <artifactId>ipm-microservices</artifactId>
        <version>1.0.0</version>
        <relativePath>../..</relativePath> <!-- adjust if needed -->
    </parent>

    <artifactId>ipm-util-validation-and-sanitization</artifactId>
    <name>IPM :: Util :: Validation and Sanitization</name>
    <description>Reusable Spring Boot utility for input validation and sanitization</description>
    <packaging>jar</packaging>

    <properties>
        <java.version>21</java.version>
        <jsoup.version>1.17.2</jsoup.version>
        <owasp.encoder.version>1.2.3</owasp.encoder.version>
    </properties>

    <dependencies>
        <!-- Spring Boot Web for REST controllers -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Bean Validation (JSR-380) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- JSoup for sanitizing HTML -->
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>${jsoup.version}</version>
        </dependency>

        <!-- OWASP Java Encoder for safe HTML/JS encoding -->
        <dependency>
            <groupId>org.owasp.encoder</groupId>
            <artifactId>encoder</artifactId>
            <version>${owasp.encoder.version}</version>
        </dependency>

        <!-- Optional: Lombok for boilerplate reduction -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>